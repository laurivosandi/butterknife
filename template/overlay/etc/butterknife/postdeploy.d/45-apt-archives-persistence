#!/bin/bash

echo "Enabling persistent APT package cache in /var/persistent/apt-archives"
mkdir -p /var/persistent/apt-archives/partial
echo "Dir::Cache::archives /var/persistent/apt-archives/;" > /etc/apt/apt.conf.d/95persistent-cache 

